# NeuroCore AI — Frontend Environment Variables
# Copy to .env and configure for your deployment

# In development, leave empty — Vite proxy handles /api/* → localhost:8000
# In production, point to your deployed FastAPI backend URL
VITE_API_URL=

